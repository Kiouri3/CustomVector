# Custom Vector Implementation in C++

Полноценная реализация контейнера `std::vector` из стандартной библиотеки C++ (STL) с полным покрытием unit-тестами. 

## 🚀 Особенности

- **Полная совместимость** с интерфейсом `std::vector`
- **Безопасность исключений** (strong exception guarantee)
- **Оптимальное управление памятью** с геометрическим расширением
- **Полный набор итераторов** (random-access, reverse, const)
- **150+ unit-тестов** из базы МФТИ
- **Кросс-платформенность** (Windows/Linux/macOS)

## 📋 Реализованный функционал

✅ Все основные конструкторы (по умолчанию, копирования, перемещения, от итераторов)  
✅ Методы доступа: `at()`, `operator[]`, `front()`, `back()`, `data()`  
✅ Модификаторы: `push_back()`, `pop_back()`, `insert()`, `erase()`, `clear()`  
✅ Управление памятью: `resize()`, `reserve()`, `shrink_to_fit()`  
✅ Итераторы: `begin()/end()`, `rbegin()/rend()`, `cbegin()/cend()`  
✅ Операторы сравнения: `==`, `!=`, `<`, `<=`, `>`, `>=`  
✅ Безопасность исключений и move-семантика  

## 🛠 Сборка и тестирование

### Требования
- CMake 3.15+
- Компилятор C++17 (GCC 9+, Clang 10+, MSVC 2019+)
- Для Windows: MSYS2/Mingw-w64 или Visual Studio

### Быстрый старт
```bash
# Клонирование репозитория
git clone https://github.com/Kiouri3/CustomVector-CPP.git
cd CustomVector-CPP

# Сборка и запуск тестов (Linux/macOS)
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make
ctest --output-on-failure

# Или с использованием Ninja
cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release
ninja
ninja test
